name: Keep Render Service Awake

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping_spitfire:
    runs-on: ubuntu-latest
    steps:
      - name: Send GET request to Spitfire
        run: curl -sS https://super-admin-one.onrender.com/api/v1/admin/shops/endpoint
